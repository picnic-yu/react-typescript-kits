{
  "name": "react-typescript-kits",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "start": "http-server dist -a 127.0.0.1 -p 8080 -o",
    "dev": "webpack-dev-server --inline --progress",
    "build": "npm run lint && webpack  --config webpack.config.prod.js",
    "lint": "tslint --force --format verbose \"src/**/*.{ts,tsx}\"",
    "test": "jest --config jest.json"
  },
  "author": "EYHN",
  "license": "GPL-2.0",
  "devDependencies": {
    "@types/jest": "21.1.5",
    "jest": "21.2.1",
    "typescript": "2.5.3",
    "ts-jest": "21.1.4",
    "babel-plugin-transform-runtime": "6.23.0",
    "babel-preset-env": "1.6.1"
  },
  "dependencies": {
    "babel-runtime": "6.26.0"
  }
}
